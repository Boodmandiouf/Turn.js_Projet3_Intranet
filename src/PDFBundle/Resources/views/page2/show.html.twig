{% extends 'base.html.twig' %}

{% block body %}
    <h1>Page2</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ page2.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('page2_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('page2_edit', { 'id': page2.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
