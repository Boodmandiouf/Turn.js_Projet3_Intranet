{% extends 'base.html.twig' %}

{% block body %}
    <h1>Page1</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ page1.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('page1_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('page1_edit', { 'id': page1.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
